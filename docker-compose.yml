version: '3.8'

services:
  web-uas:
    image: nginx:latest
    container_name: web_uas_andreana
    ports:
      - "8086:80"
    volumes:
      - ./html:/usr/share/nginx/html
    restart: always

  uptime-kuma:
    image: louislam/uptime-kuma:1
    container_name: monitoring_uas_andreana
    ports:
      - "3008:3008"
    volumes:
      - uptime-kuma-data:/app/data
    restart: always

volumes:
  uptime-kuma-data:
